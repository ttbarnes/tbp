import styled from 'styled-components';
import styles from './styles';
import HeaderLink from './HeaderLink';

export const Root = styled.div`${styles.root}`;

export const StyledHeaderLink = styled(HeaderLink)`${styles.link}`;
