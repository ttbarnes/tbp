import styled from 'styled-components';
import { Link } from 'react-router';
import styles from './styles';

export const HomeBoxRoot = styled.div`${styles.homeBoxRoot}`;

export const HomeBox = styled.div`${styles.homeBox}`;

export const StyledLink = styled(Link)`${styles.link}`;
