import React from 'react';
import {
  Root,
  ErrorMessage
} from './styled';

export default function NotFound() {
  return (
    <Root>
      <ErrorMessage>oh no :(</ErrorMessage>
    </Root>
  );
}
