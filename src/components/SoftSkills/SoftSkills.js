import TextSection from '@/components/TextSection';
import SoftSkillsList from '@/components/SoftSkillsList';

const SoftSkills = () => (
  <TextSection>
    <h2>Some of my soft skills</h2>
    <p>In no particular order</p>

    <SoftSkillsList />
  </TextSection>
);

export default SoftSkills;
